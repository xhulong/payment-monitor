package com.example.paymentmonitor

import androidx.activity.ComponentActivity

/**
 * Debug-only empty host used by Compose instrumentation tests.
 *
 * Keeping this as an explicit launcher activity avoids MIUI blocking the generic
 * ComponentActivity injected by ui-test-manifest as a background activity start.
 */
class UiTestHostActivity : ComponentActivity()
